Après analyse, j'identifie plusieurs incohérences entre les composants user account et le backend :


❌ PROBLÈMES IDENTIFIÉS :

1. user-profile.html - Champs inexistants
html<!-- CES CHAMPS N'EXISTENT PAS EN BDD ! -->
<input type="text" name="playerId" [(ngModel)]="playerId" class="form-input">
<input type="number" name="playerDays" [(ngModel)]="playerDays" class="form-input">
BDD Schema ne contient que :

username, email, description, avatar, role, createdAt

2. user-stats.ts 
privateStoriesService = CE SERVICE N'EXISTE PLUS !
private readonly privateStoriesService = inject(PrivateStoriesService);
Devrait utiliser : UserService.getStats()
Nouveau user-service. 

3. UserService endpoints - à vérifier


🔧 CORRECTIONS NÉCESSAIRES :
1. Supprimer playerId/playerDays du user-profile
2. Corriger user-stats pour utiliser UserService
3. Vérifier l'interface User dans auth.service


Corriger = ajouter ces tables dans bdd avec prisma. Comment faire ?

User stats gère les stats de user account. utiliser user service. 
Ne pas utiliser load service , load service = load histoires. 

histoires data = load service
user data = user service. 

Load ne doit pas gérer le load de stats.  
